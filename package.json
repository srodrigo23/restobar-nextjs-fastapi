{
	"scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"pnpm run api\" \"pnpm run web\"",
    "web": "cd client && pnpm run dev",
    "api": "cd service && bash scripts/api.sh"
  },
	"dependencies": {
		"concurrently": "^10.0.1"
	}
}
